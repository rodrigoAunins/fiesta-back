export enum RaffleMode {
  NUMBERED = 'numbered', // rifa clásica / números visibles
  LIST = 'list',         // evento por lista / cantidad
  SEATED = 'seated',     // evento con mapa / mesas / asientos
}